<div class="form-group">
    <label for="code" class="control-label col-sm-2">
        {{ __('Code') }}
    </label>
    <div class="col-sm-10">
        <input type="text" id="code" name="code" value="{{ {TABLE_NAME}.code }}" class="form-control" required autofocus>
    </div>
</div>

<div class="form-group">
    <label for="name" class="control-label col-sm-2">
        {{ __('Name') }}
    </label>
    <div class="col-sm-10">
        <input type="text" id="name" name="name" value="{{ {TABLE_NAME}.name }}" class="form-control" required>
    </div>
</div>

<div class="form-group">
    <label for="description" class="control-label col-sm-2">
        {{ __('Description') }}
    </label>
    <div class="col-sm-10">
        <textarea name="description" rows="4" class="form-control" required>{{ {TABLE_NAME}.description }}</textarea>
    </div>
</div>
